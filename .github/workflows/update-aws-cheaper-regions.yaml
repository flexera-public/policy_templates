name: Update AWS Cheaper Region Ratios

permissions:
  contents: write
  pull-requests: write

on:
  # Trigger immediately after the AWS EC2 pricing data is refreshed so the
  # cheaper_ratio values are always computed from the latest pricing snapshot.
  workflow_run:
    workflows: ["Generate AWS EC2 Pricing JSON"]
    types: [completed]

  # Workflow dispatch trigger allows manually running workflow
  workflow_dispatch: {}

jobs:
  update_aws_cheaper_regions:
    name: "Update AWS Cheaper Region Ratios"
    runs-on: ubuntu-latest
    # Skip if triggered by a failed upstream run; allow all manual dispatches.
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: 3.14.3
          cache: 'pip' # caching pip dependencies

      - run: pip install -r requirements.txt

      - run: python3 tools/cloud_data/aws/aws_cheaper_regions.py

      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: "Update AWS Cheaper Region Ratios"
          title: "Update AWS Cheaper Region Ratios"
          body: "Updated AWS cheaper region ratios from GitHub Actions Workflow [${{ github.workflow }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})"
          branch: "task/update-aws-cheaper-regions"
          delete-branch: true
          labels: "automation, data, aws"

      - name: Check outputs
        if: ${{ steps.cpr.outputs.pull-request-number }}
        run: |
          echo "Pull Request Number - ${{ steps.cpr.outputs.pull-request-number }}"
          echo "Pull Request URL - ${{ steps.cpr.outputs.pull-request-url }}"
