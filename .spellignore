# Spell check ignore list.
# Each line is a word that will be ignored by spell check.
# Lines that begin with #, like these, are ignored.
rightscale
flexera
datasource
datasources
github
YYYY-MM
Microsoft.Resources/subscriptions/resources/read
Microsoft.Resources/subscriptions/providers/read
Microsoft.Resources/resourceGroups/read
provider=gce
provider=azure_rm
/^[a-zA-Z0-9]+=[a-zA-Z0-9]+$/
